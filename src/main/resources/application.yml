spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coupang
    username: root
    password:  1234 # password123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 테이블을 자동 생성해주는 옵션
    show-sql: true

  elasticsearch:
    uris: http://54.116.98.79:9200

  # 👇 이 부분을 새롭게 추가해 줍니다!
  output:
    ansi:
      enabled: always

logging:
  level:
    org.elasticsearch.client: TRACE